<?xml version="1.0" encoding="utf-8"?>
<!-- HTTPS everywhere; plain HTTP only towards this device itself (the Dropbox
     sign-in comes back on http://localhost:53682, and a WebDAV server reached
     through adb reverse / Termux) and the emulator host used by Metro in debug. -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
